<table>
  <tr>
    <td>{{credential.issuanceDate}}</th>
    <td>{{credential.type}}</th>
    <td>{{credential.issuer.name}}</th>
    <td>{{credential.subject.name}}</th>
  </tr>
</table>


{{#each credential.claims}}
{{type}} <b>{{value}}</b>
{{~/each}}

